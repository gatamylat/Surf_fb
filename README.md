# Surf — Многопользовательская версия

## Что добавлено

| Фича | Описание |
|------|----------|
| 🔐 Экран входа | Личный / Совместный режим |
| ☁️ Firebase sync | Все данные в облаке (tasks, projects, ritual, notes, personal) |
| ⚡ Realtime | В совместном режиме изменения видны мгновенно |
| 👥 Пользователи | Список участников в шапке |
| 📡 Статус | Зелёная точка = подключен, жёлтая = синхронизация, красная = офлайн |
| 🔄 Синхронизация | Кнопка ручной синхронизации в меню |
| ⬇️ Загрузить из облака | Принудительная загрузка (перезаписывает локальное) |
| 🚪 Выход | Смена пользователя / комнаты |

## Как использовать

### Личный режим (для себя с разных устройств)
1. Открой приложение
2. Введи своё имя (например «Пу»)
3. Нажми «Начать»
4. На другом устройстве — введи **то же самое имя**
5. Данные синхронизируются через Firebase

### Совместный режим (с другим человеком)
1. Открой приложение → вкладка «Совместный»
2. Введи своё имя
3. Оставь код пустым → создастся новая комната
4. Поделись кодом комнаты с другим человеком
5. Он вводит тот же код → видит те же данные
6. Изменения в реальном времени

## Деплой на GitHub Pages

### Все файлы (загрузить ВСЕ в репозиторий):

```
surf/
├── index.html       ← приложение
├── sw.js            ← Service Worker (офлайн)
├── manifest.json    ← PWA манифест
├── icon-192.png     ← иконка
├── icon-512.png     ← иконка
└── .nojekyll        ← ОБЯЗАТЕЛЬНО
```

### Что такое `.nojekyll` и как создать:

`.nojekyll` — это пустой файл. Он говорит GitHub: «не обрабатывай мой сайт через Jekyll». Без него GitHub ломает файлы с подчёркиваниями в имени.

**Как создать:** он уже есть в скачанных файлах! Просто загрузи его вместе с остальными. Если нужно создать вручную:
- На GitHub: нажми «Add file» → «Create new file» → напиши имя `.nojekyll` → оставь содержимое пустым → «Commit»
- На компьютере: создай пустой текстовый файл и назови его `.nojekyll` (без расширения)

### Шаги:

1. Создай репозиторий (например `surf`)
2. Загрузи **ВСЕ 6 файлов** (включая `.nojekyll`)
3. Settings → Pages → Source: main branch
4. Подожди 2 минуты
5. Открой `https://gatamylat.github.io/surf/`
6. F12 → Console → должно быть `✅ Firebase OK` и `📦 SW registered`

## Firebase

API ключ уже вставлен — проект `skills-tracker-3f21b`.
Правила Firestore должны разрешать `/apps/{appId}/{type}/{docId}`.

## Структура данных в Firebase

```
apps/
  surf-tasks/
    users/
      u_abc123/     ← личный режим (хеш от имени)
        data        ← tasks, projects, ritual, notes, personalTasks
    rooms/
      k7x2m9/      ← совместный режим (код комнаты)
        data        ← общие данные + список участников
```

## Service Worker

SW использует стратегию **network-first** (сеть первая):
- Есть интернет → берёт свежую версию с сервера
- Нет интернета → берёт из кеша (офлайн-режим)

При обновлении кода на GitHub: открой `sw.js` и поменяй `surf-v1` на `surf-v2` (и так далее). Это сбросит кеш.

## Меню (три точки)

- 🌙 Ночной режим
- ☁️ Синхронизация — загрузить данные из облака
- ⬇️ Загрузить из облака — принудительная перезапись
- 📡 Статус Firebase — отладочная информация
- 📤 Экспорт текст
- 💾 Экспорт JSON
- 📥 Импорт JSON
- 🚪 Выйти — смена пользователя
